# 🚀 Quick Start Guide - EduSports User Management\n\n## ⚡ **Chạy Hệ Thống Trong 5 Phút**\n\n### 📋 **Prerequisites**\n- Java 17+\n- Node.js 16+\n- MySQL 8.0+\n\n### 🎯 **Step 1: Setup Database (1 phút)**\n\n```sql\n-- Tạo database\nCREATE DATABASE edusports_db;\nUSE edusports_db;\n\n-- Thêm columns vào bảng users (nếu chưa có)\nALTER TABLE users ADD COLUMN last_login TIMESTAMP NULL;\nALTER TABLE users ADD COLUMN phone VARCHAR(20) NULL;\nALTER TABLE users ADD COLUMN avatar VARCHAR(255) NULL;\n\n-- Tạo bảng password_reset_tokens\nCREATE TABLE password_reset_tokens (\n    id BIGINT AUTO_INCREMENT PRIMARY KEY,\n    token VARCHAR(255) NOT NULL UNIQUE,\n    user_id BIGINT NOT NULL,\n    expires_at TIMESTAMP NOT NULL,\n    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    used BOOLEAN NOT NULL DEFAULT FALSE,\n    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,\n    INDEX idx_token (token),\n    INDEX idx_user_id (user_id),\n    INDEX idx_expires_at (expires_at)\n);\n```\n\n### 🎯 **Step 2: Start Backend (1 phút)**\n\n```bash\n# Navigate to backend\ncd C:\\Users\\ACER\\Desktop\\edusports\n\n# Build and run\n./gradlew clean build\n./gradlew bootRun\n\n# Verify backend is running\n# Open browser: http://localhost:8080/api/v1/auth/account\n# Should see \"User not authenticated\" response\n```\n\n### 🎯 **Step 3: Start Frontend (1 phút)**\n\n```bash\n# Navigate to frontend\ncd C:\\Users\\ACER\\Desktop\\fe\\fe-edu\n\n# Install dependencies (if needed)\nnpm install\n\n# Start development server\nnpm start\n\n# Frontend should open at http://localhost:3000\n```\n\n### 🎯 **Step 4: Create Admin User (1 phút)**\n\n#### **Option A: Via Database** (Recommended)\n```sql\n-- Insert admin user directly\nINSERT INTO users (name, email, password, is_active, created_at) \nVALUES ('Admin User', 'admin@gmail.com', '$2a$10$Hash...', true, NOW());\n\n-- Get admin user ID\nSET @admin_id = LAST_INSERT_ID();\n\n-- Insert admin role\nINSERT INTO role (name) VALUES ('ADMIN') ON DUPLICATE KEY UPDATE id=id;\n\n-- Get admin role ID  \nSET @admin_role_id = (SELECT id FROM role WHERE name = 'ADMIN');\n\n-- Assign admin role to user\nINSERT INTO user_roles (user_id, role_id) VALUES (@admin_id, @admin_role_id);\n```\n\n#### **Option B: Via Registration + Manual Role Assignment**\n1. Register at `/register` với email `admin@gmail.com`\n2. Update role trong database:\n   ```sql\n   UPDATE user_roles SET role_id = (SELECT id FROM role WHERE name = 'ADMIN') \n   WHERE user_id = (SELECT id FROM users WHERE email = 'admin@gmail.com');\n   ```\n\n### 🎯 **Step 5: Test User Management (1 phút)**\n\n1. **Login:** Go to `http://localhost:3000/login`\n   - Email: `admin@gmail.com`\n   - Password: `your_password`\n\n2. **Access Admin Panel:** Click \"Admin Panel\" trong navigation\n\n3. **Open User Management:** Click \"User Management\" tab\n\n4. **Test Features:**\n   - ✅ View user list\n   - ✅ Search users\n   - ✅ Create new user\n   - ✅ Edit user details\n   - ✅ View user statistics\n\n## 🎯 **Quick Test Checklist**\n\n### ✅ **Basic Functionality**\n- [ ] Backend starts without errors\n- [ ] Frontend loads successfully\n- [ ] Admin login works\n- [ ] User Management page loads\n- [ ] User list displays\n- [ ] Statistics show correct numbers\n\n### ✅ **CRUD Operations**\n- [ ] Create User: Click \"Add User\", fill form, submit\n- [ ] View User: Click eye icon on any user\n- [ ] Edit User: Click edit icon, modify data, save\n- [ ] Delete User: Click delete icon, confirm\n- [ ] Toggle Status: Click status toggle button\n\n### ✅ **Advanced Features**\n- [ ] Search: Type in search box, results filter\n- [ ] Filter by Role: Select role dropdown\n- [ ] Filter by Status: Select status dropdown\n- [ ] Sort: Click column headers\n- [ ] Pagination: Navigate between pages\n- [ ] Export: Click \"Export CSV\" button\n- [ ] Bulk Delete: Select multiple users, delete\n\n## 🐛 **Troubleshooting**\n\n### **Backend Issues:**\n```bash\n# Check if backend is running\ncurl http://localhost:8080/api/v1/auth/account\n\n# Check logs\ntail -f logs/spring.log\n\n# Restart backend\n./gradlew bootRun\n```\n\n### **Frontend Issues:**\n```bash\n# Clear cache and restart\nnpm start -- --reset-cache\n\n# Check console for errors\n# Open browser dev tools → Console tab\n\n# Check network requests\n# Open browser dev tools → Network tab\n```\n\n### **Database Issues:**\n```sql\n-- Check tables exist\nSHOW TABLES;\n\n-- Check user data\nSELECT u.*, r.name as role FROM users u \nLEFT JOIN user_roles ur ON u.id = ur.user_id \nLEFT JOIN role r ON ur.role_id = r.id;\n\n-- Check password_reset_tokens table\nDESC password_reset_tokens;\n```\n\n### **Authentication Issues:**\n1. **Can't login:** Check password encoding trong database\n2. **No admin access:** Verify ADMIN role assignment\n3. **Token errors:** Clear localStorage và login again\n4. **CORS errors:** Check backend CORS configuration\n\n## 📞 **Quick API Tests**\n\n### **Test với Postman/curl:**\n\n```bash\n# 1. Login để get token\ncurl -X POST http://localhost:8080/api/v1/auth/login \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"username\":\"admin@gmail.com\",\"password\":\"your_password\"}'\n\n# 2. Test user list (replace TOKEN)\ncurl -X GET http://localhost:8080/api/v1/admin/users \\\n  -H \"Authorization: Bearer YOUR_TOKEN\"\n\n# 3. Test user creation\ncurl -X POST http://localhost:8080/api/v1/admin/users \\\n  -H \"Authorization: Bearer YOUR_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\":\"Test User\",\"email\":\"test@test.com\",\"password\":\"password123\",\"roles\":[\"STUDENT\"]}'\n```\n\n## ⚡ **Performance Check**\n\n### **Expected Response Times:**\n- User list load: < 500ms\n- User create/update: < 300ms\n- Search/filter: < 200ms\n- Statistics load: < 100ms\n\n### **Browser Performance:**\n- Open dev tools → Performance tab\n- Record user interactions\n- Check for performance bottlenecks\n\n## 🎉 **Success Indicators**\n\nIf you see these, everything is working perfectly:\n\n- ✅ **Backend:** Console shows \"Started CheckscamApplication\"\n- ✅ **Frontend:** Page loads với no console errors\n- ✅ **Database:** Users table has data\n- ✅ **Authentication:** Login redirects to dashboard\n- ✅ **User Management:** Table shows users với statistics\n- ✅ **API Integration:** CRUD operations work smoothly\n\n## 🚀 **Next Steps**\n\nOnce basic functionality works:\n\n1. **Customize UI:** Modify colors, layouts trong `src/index.css`\n2. **Add Features:** Extend UserManagement component\n3. **Setup Production:** Configure environment variables\n4. **Deploy:** Setup CI/CD pipeline\n5. **Monitor:** Add logging và analytics\n\n---\n\n**🎊 Chúc mừng! User Management System đã sẵn sàng hoạt động!**\n\n*Need help? Check FRONTEND_INTEGRATION_COMPLETE.md for detailed documentation.*\n